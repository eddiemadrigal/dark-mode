import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles ( theme => ({

    componentBackground: {
        marginTop: "30px",
        background: "#333",
        paddingTop: "30px",
        paddingBottom: "30px",
        borderRadius: "10px"
    }

}));